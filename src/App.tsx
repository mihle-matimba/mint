import BottomNavBar from "@/components/ui/bottom-nav-bar";

const App = () => {
  return (
    <div className="min-h-screen bg-slate-50 text-slate-900">
      <div className="p-6 text-lg font-semibold">Mint App</div>
      <BottomNavBar />
    </div>
  );
};

export default App;
